/*const SearchCountrie = ({name}) => {
    return (
        <form>
            <datalist id="buscador">
                {countries.map((countrie) => <option value = {countrie.name.common}></option>)}
            </datalist>
            <label>
                <input list = "buscador" name="buscador"></input>
            </label>
      </form>
    )
}

export default SearchCountrie*/